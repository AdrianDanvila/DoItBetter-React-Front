export const Path = {
  home: '/',
  login: '/login',
  register: '/register',
  main: '/main',
  routines: '/routines',
  profile: '/profile',
}

export const RoutePath = {
  home: Path.home,
  main: Path.main,
  login: Path.login,
  routines: Path.routines,
  profile: Path.profile,
}
