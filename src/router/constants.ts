export const Path = {
  home: '/',
  login: '/login',
  register: '/register',
  main: '/main',
  routines: '/routines',
}

export const RoutePath = {
  home: Path.home,
  main: Path.main,
  routines: Path.routines,
}
